# rust-log-tail

A CLI tool that tails the last N lines of a file and optionally follows it for new entries.

## Stack

Primary language: **Rust**

## Project structure

- `src/main.rs` — Tail and follow log files from the command line.
- `Cargo.toml` — dependencies

## Usage

Review the source files and install any dependencies referenced by the project before running it.
